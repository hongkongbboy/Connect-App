# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Konnect' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Konnect
  pod "Parse"
  pod "AlamofireImage"

  target 'KonnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'KonnectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
